---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: storagespacemonitors.monitor.storage-spaces.monitor.com
spec:
  group: monitor.storage-spaces.monitor.com
  names:
    kind: StorageSpaceMonitor
    listKind: StorageSpaceMonitorList
    plural: storagespacemonitors
    singular: storagespacemonitor
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageSpaceMonitor is the Schema for the storagespacemonitors
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StorageSpaceMonitorSpec defines the desired state of StorageSpaceMonitor.
            properties:
              pools:
                description: Pools is a list of storage pool names to monitor
                items:
                  type: string
                type: array
            type: object
          status:
            description: "StorageSpaceMonitorStatus defines the observed state of
              StorageSpaceMonitor.\nExample pool status:\nGet-StoragePool | Select-Object
              FriendlyName, HealthStatus | ConvertTo-Json\n[\n\n\t{\n\t\t\"FriendlyName\":
              \"Plex Entertainment Pool\",\n\t\t\"HealthStatus\": \"Healthy\"\n\t},\n\t{\n\t\t\"FriendlyName\":
              \"Cloud Pool\",\n\t\t\"HealthStatus\": \"Healthy\"\n\t},\n\t{\n\t\t\"FriendlyName\":
              \"Primordial\",\n\t\t\"HealthStatus\": \"Healthy\"\n\t}\n\n]"
            properties:
              poolStatuses:
                description: PoolStatuses contains the health status of monitored
                  pools
                items:
                  description: |-
                    StorageSpaceMonitorStatus defines the observed state of StorageSpaceMonitor.
                    PoolStatus represents the health status of a storage pool
                  properties:
                    friendlyName:
                      type: string
                    healthStatus:
                      type: string
                  required:
                  - friendlyName
                  - healthStatus
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
